// ====== 通用类型
let dNumber = 1; // number
let dListNumber = [1, 2, null]; // (number | null)[]

// ====== 上下文归类

window.addEventListener("click", (event) => {}); // MouseEvent

window.addEventListener("scroll", (event) => {}); // Event

window.addEventListener("mousedown", (event) => {}); // MouseEvent
